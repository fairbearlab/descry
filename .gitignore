# System
.DS_Store
Thumbs.db

# Riptide artifacts (cloud-synced)
.humanlayer/
.humanlayer/tasks/
.gstack/
.claude/

# Internal seam docs — they name the private consumer; must NOT ship in this
# public, consumer-agnostic module. Public seam contract: docs/ARCHITECTURE.md
docs/internal/

# Compiled binary
/descry

# Test coverage profile (written by the CI test command when run locally)
coverage.out
